<h2>Interns & Staff</h2>

<h4>Staff</h4>
<ul class="employees">
  <%= render :partial => 'employees/employee', :collection => @staff %>
</ul>

<h4>Interns</h4>
<ul class="employees">
  <%= render :partial => 'employees/employee', :collection => @interns %>
</ul>
<!--
<table>
  <tr>
    <th>Avatar</th>
    <th>Name</th>
    <th>Role</th>
    <th>Department</th>
    <th>Bio</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @employees.each do |employee| %>
  <tr>
    <td><%= image_tag employee.avatar.url(:thumb) %></td>
    <td><%= employee.name %></td>
    <td><%= employee.role %></td>
    <td><%= employee.department %></td>
    <td><%= employee.bio %></td>
    <td><%= link_to 'Show', employee %></td>
    <td><%= link_to 'Edit', edit_employee_path(employee) %></td>
    <td><%= link_to 'Edit Thumbnail', crop_employee_path(employee) %></td>
    <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
-->
<br />

<%= link_to 'New Employee', new_employee_path %>
